services:
  backend:
    container_name: spring-backend
    build:
      context: ./backend
    ports:
      - "8080:8080"
    restart: always

  frontend:
    container_name: react-frontend
    build:
      context: ./frontend
    ports:
      - "80:80"
    restart: always