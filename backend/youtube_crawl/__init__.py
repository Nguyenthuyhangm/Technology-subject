"""YouTube Video Crawler — crawl short video reviews for products.

Does NOT download video files — only fetches metadata (videoId, title, thumbnail, link)
and saves to product_video + product_video_mapping tables.
"""
